def quick_sort(arr, left, right):
    """Sorts an array using the quick sort algorithm.
    Args:
        arr (list): The array to be sorted.
    Returns:
        list: A sorted array
    
    >>> quick_sort(arr=[38, 27, 43, 3, 9, 82, 10], 0, len(arr) -1 )
    [3, 9, 10, 27, 38, 43, 82]
     """
    if left < right:
       # Find pivot position

       # recursive call left

       # recursive call right
       pass

    return arr
    

def quick_helper(arr, left, right)-> int:
    """
    Args: 
        arr (list): The unsorted array/sub-array
        left (int): The left most index
        right (int): The right most index

    Returns: 
        int: The pivot position 
    """

# Test code bellow
if __name__ == "__main__":
    my_lst = [38, 27, 43, 3, 9, 82, 10]
    pass